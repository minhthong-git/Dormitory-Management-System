{
  "name": "dormitory-management-system",
  "version": "1.0.0",
  "description": "Production-ready Dormitory Management System — Decoupled Fullstack Monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER -c cyan,yellow \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "concurrently \"npm run build:client\" \"npm run build:server\"",
    "build:client": "npm run build --prefix client",
    "build:server": "npm run build --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "lint": "concurrently \"npm run lint --prefix client\" \"npm run lint --prefix server\"",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\" --ignore-path .gitignore",
    "clean": "rimraf client/node_modules server/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
