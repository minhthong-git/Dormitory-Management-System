import React from 'react';

const RegisterPage: React.FC = () => (
  <div className="auth-page">
    <div className="auth-card">
      <h1 className="auth-title">Đăng ký tài khoản</h1>
      <p>Trang đăng ký — implement sau.</p>
    </div>
  </div>
);

export default RegisterPage;
